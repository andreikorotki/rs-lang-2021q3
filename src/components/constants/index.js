import { bgColors } from './bgColors';
import { DIFFICULTIES } from './Difficulties';
import { WORDS_LEARNED } from './learned';
import { BUTTONS_GROUP, BUTTONS_GAME, HARD_GROUP, BOOK_INITIAL_STATE } from './book';
import { SPRINT_STATE, SPRINT_LEVELS, PRIZE_POINTS } from './sprint';

export {
  bgColors,
  BUTTONS_GROUP,
  BUTTONS_GAME,
  HARD_GROUP,
  BOOK_INITIAL_STATE,
  DIFFICULTIES,
  WORDS_LEARNED,
  SPRINT_STATE,
  SPRINT_LEVELS,
  PRIZE_POINTS
};
